#!/usr/bin/env sh
set -eu

HOOK_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$HOOK_DIR/.."

npm run security:check
npm run build:client
npm run assets:manifest
git add -- public/assets/asset-manifest.json
npm run assets:manifest:check
